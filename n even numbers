#include<stdio.h>
int main()
{
    int i, n;
    printf("Enter maximum number : ");
    scanf("%d", &n);
    printf("Even Numbers between 1 and %d : \n", n);
    for(i =1; i<=n;i++)
    {
        if(i%2==0)
        {
            printf("%d\t",i);
        }
    }
    return 0;
}
